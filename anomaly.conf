# Anomaly Detection Configuration
# Lines starting with # are comments.
# Format: key=value

# Enable/disable checks (1=on, 0=off)
check_large_packets=1
check_malformed_headers=1
check_suspicious_ports=1
check_unknown_protocols=1
check_arp_spoofing=1

# Threshold for large packet detection (bytes)
large_packet_threshold=1500

# Suspicious ports (one per line)
suspicious_port=23
suspicious_port=21
suspicious_port=513
suspicious_port=3389
suspicious_port=5900
